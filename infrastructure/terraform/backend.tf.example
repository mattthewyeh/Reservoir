terraform {
  backend "s3" {
    bucket       = "replace-with-your-terraform-state-bucket"
    key          = "reservoir/production/terraform.tfstate"
    region       = "us-west-2"
    encrypt      = true
    use_lockfile = true
  }
}
